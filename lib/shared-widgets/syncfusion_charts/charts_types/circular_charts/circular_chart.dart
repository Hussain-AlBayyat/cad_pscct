import 'package:flutter/material.dart';

class SyncfusionCircularChart extends StatelessWidget {
  const SyncfusionCircularChart({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
