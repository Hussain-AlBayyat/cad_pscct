import 'package:flutter/material.dart';

import '../../../shared-widgets/syncfusion_charts/charts_types/data_grid_chart.dart';

class OutsourcedInventory extends StatelessWidget {
  OutsourcedInventory({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SyncfusionDataGridChart();
  }
}
